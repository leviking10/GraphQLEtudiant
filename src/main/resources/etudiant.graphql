schema{
    query: Query
}

type Query{
    getAllEtudiant : [Etudiant]
    findEtudiant(email: String) : Etudiant
}

type Etudiant{
    id: Int
    name: String
    telephone: String
    email: String
    address: String
}
